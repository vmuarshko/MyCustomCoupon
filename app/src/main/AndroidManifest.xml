<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zprocessapps.mycustomcoupon">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".objects.C_Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddressActivity"
            android:label="@string/app_name">

        </activity>
        <activity
            android:name=".BusinessDataActivity"
            android:label="My Custom Coupon" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".CouponListActivity"
            android:label="@string/title_coupon_list" />
        <!-- <activity -->
        <!-- android:name=".objects.CouponDetailActivity" -->
        <!-- android:label="@string/title_coupon_detail" -->
        <!-- android:parentActivityName=".CouponListActivity"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value="com.zprocessapps.mycustomcoupon.CouponListActivity" /> -->
        <!-- </activity> -->
        <activity
            android:name=".RequestListActivity"
            android:label="@string/title_request_list" />
        <!-- <activity -->
        <!-- android:name=".objects.RequestDetailActivity" -->
        <!-- android:label="@string/title_request_detail" -->
        <!-- android:parentActivityName=".RequestListActivity"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value="com.zprocessapps.mycustomcoupon.RequestListActivity" /> -->
        <!-- </activity> -->
        <activity
            android:name=".ReportActivity"
            android:label="@string/title_activity_report"></activity>
    </application>

</manifest>